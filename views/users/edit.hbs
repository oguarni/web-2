<div class="d-flex justify-content-between align-items-center mb-4">
    <h1><i class="fas fa-user-edit me-2"></i>Editar Usuário</h1>
    <div class="btn-group">
        <a href="/usuarios" class="btn btn-secondary">
            <i class="fas fa-arrow-left me-2"></i>Voltar
        </a>
        <a href="/usuarios/{{usuario.id}}" class="btn btn-info">
            <i class="fas fa-eye me-2"></i>Ver Detalhes
        </a>
    </div>
</div>

<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-body">
                <form method="POST" action="/usuarios/{{usuario.id}}?_method=PUT">
                    <div class="mb-3">
                        <label for="nome" class="form-label">Nome <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" id="nome" name="nome" value="{{usuario.nome}}" required>
                    </div>
                    
                    <div class="mb-3">
                        <label for="login" class="form-label">Login <span class="text-danger">*</span></label>
                        <input type="text" class="form-control" id="login" name="login" value="{{usuario.login}}" required>
                        <div class="form-text">Login único para acesso ao sistema</div>
                    </div>
                    
                    <div class="mb-3">
                        <label for="senha" class="form-label">Nova Senha</label>
                        <input type="password" class="form-control" id="senha" name="senha">
                        <div class="form-text">Deixe em branco para manter a senha atual</div>
                    </div>
                    
                    <div class="mb-3">
                        <label for="tipo" class="form-label">Tipo de Usuário</label>
                        <select class="form-select" id="tipo" name="tipo">
                            <option value="2" {{#if (eq usuario.tipo 2)}}selected{{/if}}>Usuário Comum</option>
                            <option value="3" {{#if (eq usuario.tipo 3)}}selected{{/if}}>Gestor</option>
                            <option value="1" {{#if (eq usuario.tipo 1)}}selected{{/if}}>Administrador</option>
                        </select>
                        <div class="form-text">
                            <small>
                                <strong>Comum:</strong> Pode criar e gerenciar suas próprias reservas<br>
                                <strong>Gestor:</strong> Pode gerenciar espaços e ver todas as reservas<br>
                                <strong>Admin:</strong> Acesso total ao sistema
                            </small>
                        </div>
                    </div>
                    
                    <div class="d-grid gap-2 d-md-flex justify-content-md-end">
                        <a href="/usuarios/{{usuario.id}}" class="btn btn-secondary">
                            <i class="fas fa-times me-2"></i>Cancelar
                        </a>
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save me-2"></i>Salvar Alterações
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-md-4">
        <div class="card">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="fas fa-info-circle me-2"></i>Informações
                </h5>
                <div class="text-muted">
                    <p><strong>Usuário ID:</strong> {{usuario.id}}</p>
                    <p><strong>Tipo atual:</strong> 
                        <span class="badge bg-{{#if (eq usuario.tipo 1)}}danger{{else}}{{#if (eq usuario.tipo 3)}}warning{{else}}success{{/if}}{{/if}}">
                            {{tipoUsuario usuario.tipo}}
                        </span>
                    </p>
                    
                    <hr>
                    
                    <p><strong>Campos obrigatórios:</strong></p>
                    <ul>
                        <li>Nome completo</li>
                        <li>Login único</li>
                    </ul>
                    
                    <p><strong>Senha:</strong></p>
                    <ul>
                        <li>Deixe em branco para manter</li>
                        <li>Ou digite nova senha (min. 6 caracteres)</li>
                    </ul>
                    
                    <p><strong>Tipos de usuário:</strong></p>
                    <ul>
                        <li><span class="badge bg-success">Comum</span> - Reservas próprias</li>
                        <li><span class="badge bg-warning">Gestor</span> - Gerencia espaços</li>
                        <li><span class="badge bg-danger">Admin</span> - Acesso total</li>
                    </ul>
                </div>
            </div>
        </div>
        
        <div class="card mt-3">
            <div class="card-body">
                <h5 class="card-title">
                    <i class="fas fa-exclamation-triangle me-2 text-warning"></i>Atenção
                </h5>
                <div class="text-muted">
                    <p><strong>Alteração de tipo:</strong></p>
                    <p class="small">
                        Alterar o tipo de usuário afeta imediatamente as permissões.
                        Certifique-se de que o usuário deve ter essas permissões.
                    </p>
                </div>
            </div>
        </div>
    </div>
</div>